<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>To Do List</h1>
    <p id="total-tasks">Total Tasks Remaining: <%= uncompletedTasks %></p>

    <ul id="list-items">
        <% tasks.forEach(task => { %>
            <div class="task-container">
                <li class="task-item">
                    <% if(task.complete === true) {%>
                        <span class="complete"><%= task.name %></span>
                        <% }else{ %>
                        <span><%= task.name %></span>
                        <% } %>
                    <i class="fa fa-trash delete-button" data-name="<%= task.name %>" ></i>
                </li>
            </div>
        <%});%>
    </ul>

    <p>Add a New Task</p>
    <form action="/new-task" method="POST">
        <input type="text" placeholder="add a task" name="todoItem">
        <button type="submit" action="POST">Submit</button>
    </form>


    <script src="/main.js"></script>
</body>
</html>